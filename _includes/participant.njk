<div class="card">
  <img src="{{ participant.image }}" alt="{{ participant.name }}">
  <h2>{{ participant.name }}</h2>
  <p><strong>Carrera:</strong> {{ participant.career }}</p>
  <p><strong>Descripción:</strong> {{ participant.description }}</p>
  <p><strong>Hobbies:</strong> {{ participant.hobbies }}</p>
</div>